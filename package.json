{
  "name": "securevpn-mobile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "cap:init": "npx cap init SecureVPN com.securevpn.app --web-dir=dist",
    "cap:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.8",
    "@capacitor/core": "^5.7.8"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.8",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}